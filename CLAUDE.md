@AGENTS.md

# Claude Code notes

- Use the project skills under `.claude/skills/speckit-*` for Spec Kit commands.
- Store shared project decisions in the corresponding `specs/` artifacts or `.specify/memory/constitution.md`, not in Claude-only memory.
- Keep personal settings in `.claude/settings.local.json` and do not commit them.
