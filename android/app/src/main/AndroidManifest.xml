<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- F006 진행 시작 시 앱 사용 중 1회 위치. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
      F007 자동 감지. 사용자가 앱을 열어 두지 않아도 지오펜스가 발화해야 하므로
      백그라운드 위치가 필요하다(spec Assumptions).

      Android 11 이상은 이 권한을 앱 사용 중 권한과 같은 화면에서 함께 요청할 수 없다.
      진행을 시작한 뒤 자동 감지를 켤 때 별도로 요청하고, 거부해도 자동 감지만 끄고
      F006 수동 진행은 그대로 둔다(spec FR-024, research.md 8절).
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gilpick">

        <!--
          Naver Maps NCP Key ID. build.gradle.kts가 GILPICK_NAVER_MAPS_CLIENT_ID property에서
          채우므로 source에 값을 두지 않는다.
        -->
        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="${naverMapsClientId}" />

        <!--
          F007 지오펜스 broadcast 수신 지점. Play Services가 프로세스를 깨워 전달하므로
          앱이 실행 중이 아니어도 도착·출발 이벤트를 받는다. 앱 안에서만 쓰이므로
          외부에 노출하지 않는다.
        -->
        <receiver
            android:name=".progress.GeofenceReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              인증 완료 path만 verified App Link로 claim한다. 같은 host의 API callback
              path(/api/v1/auth/kakao/callback)는 Kakao가 브라우저로 호출해야 하므로
              claim하지 않는다.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${appLinkHost}"
                    android:path="/auth/kakao/complete" />
            </intent-filter>
        </activity>
    </application>
</manifest>
