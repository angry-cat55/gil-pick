org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# App Link host (non-secret). Real contest domain is injected via -P or CI env
# GILPICK_ANDROID_APP_LINK_HOST at release build time; this is a placeholder
# for local/debug builds only. See specs/001-kakao-auth/quickstart.md.
GILPICK_ANDROID_APP_LINK_HOST=app.gilpick.example
GILPICK_API_BASE_URL=https://api.gilpick.example/api/v1/
